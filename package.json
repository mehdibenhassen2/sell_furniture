{
  "scripts": {
    "build": "cd sell_furniture && npm install && npm run build && mkdir -p ../dist && cp -r dist/sell_furniture ../dist/ && cp ../index.html ../dist/"
  },
  "devDependencies": {
    "@angular/cli": "^19.2.15"
  }
}
