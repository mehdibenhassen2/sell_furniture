{
  "scripts": {
    "build": "cd sell_furniture && npm install && npm run build && mkdir -p ../dist/sell_furniture && cp -r dist/sell_furniture/browser ../dist/sell_furniture/"
  },
  "devDependencies": {
    "@angular/cli": "^19.2.15"
  },
  "dependencies": {
    "@cloudinary/angular-5.x": "^1.5.4"
  }
}
