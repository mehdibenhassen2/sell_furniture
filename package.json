{
  "scripts": {
    "build": "cd sell_furniture && npm install && npm run build && mkdir -p ../dist/sell_furniture/browser && cp -r dist/sell_furniture/browser/* ../dist/sell_furniture/browser/"
  },
  "devDependencies": {
    "@angular/cli": "^19.2.15"
  }
}
